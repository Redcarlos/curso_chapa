function mySum(a, b) {
  // Code to be executed
  return a + b;
}

const myRest = (a, b) => a - b;

const hardFunction = (param1, param2) => {
  const operation = param1 + param2;
  for (i = 0; i < operation; i++) {
    console.log(i);
  }
};

const today = new Date().getDay();

console.log(today);
